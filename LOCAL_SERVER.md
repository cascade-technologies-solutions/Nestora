Local development server for Nestora

Dev URL: http://localhost:5174/Nestora

Notes:
- Start with `npm run dev` in project root.
- If PowerShell blocks scripts, run from Command Prompt or set `Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned`.
